# 功能基线

产品能力的上层索引记录在[能力地图](./能力地图.md)。具体知识分为[需求](./需求/README.md)和[功能](./功能/README.md)：需求说明为什么做，功能说明系统应表现出的行为。两者是多对多关系，由功能使用 `rel_satisfies` 指向需求。

旧版 [功能卡模板](./TEMPLATE.md) 只用于一个格式版本的兼容读取；新建功能必须使用 `功能/` 下的模板和 `F-<领域>-<三位序号>` 编号。

候选内容先以 Proposal 呈现；确认前状态不得升级为现行基线。未知验收条件要标记待确认，不能编造。
