# 接口

HTTP、RPC、事件、Webhook、文件和稳定函数接口使用统一 `interface` 知识类型，一项契约一文件并在本目录平铺。通过 `interface_kind` 区分通信方式，通过 `visibility` 区分内部与外部接口。

接口文件是输入、输出、错误、敏感字段、版本和兼容策略的唯一项目定义。提供方和调用方通过关系引用稳定 ID，不复制契约正文；反向消费者由检查器计算。
