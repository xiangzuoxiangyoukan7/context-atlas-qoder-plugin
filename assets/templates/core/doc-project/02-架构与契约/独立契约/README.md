# 独立契约

本目录只保存至少满足一项独立性条件的契约：被多个知识项复用、跨模块或系统边界、拥有独立版本、拥有独立责任方，或在所属单一功能退出后仍然有效。文件必须通过 `independence_basis` 声明依据，文件名为 `CONTRACT-<领域>-<三位序号>-<名称>.md`。仅属于一个功能、接口、模块或数据对象的局部约束必须写回所属文件；“需要单独验收”本身不构成独立理由。

只属于单个知识项且没有独立生命周期的局部约束，应写在对应知识文件正文。跨项目公共标准保留权威来源、采用版本和本地差异，不复制为可独立漂移的权威副本。
