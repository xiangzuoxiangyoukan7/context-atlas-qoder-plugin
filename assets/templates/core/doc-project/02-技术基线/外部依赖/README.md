---
id: IDX-DEPENDENCIES
type: knowledge_index
title: 外部依赖
rel_classified_under:
  - "[[02-技术基线/README|IDX-TECHNICAL-BASELINE]]"
---
# 外部依赖

## 目录契约

本目录只保存外部系统、服务或组件依赖，不保存项目内部模块或临时调研。依赖项使用稳定身份命名，并以 `rel_classified_under` 指向本 README。使用 `children` 查看目录内容、`neighbors` 查询直接成员；普通 `graph` 到达本 README 后停止，只有显式分类成员查询才继续展开。

| 依赖编号 | 名称 | 用途 | 版本/端点 | 可用性与降级 | 许可证/责任人 | 来源 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- |

不存放凭据；仅记录环境变量名称和配置位置。版本、SLA 或许可证未知时保持 `missing`。
