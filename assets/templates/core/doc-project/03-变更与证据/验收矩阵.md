# 验收矩阵

| 验收编号 | 对象 | 条件摘要 | 结果 | 证据位置 | 对应版本 |
| --- | --- | --- | --- | --- | --- |

每个功能和任务声明的验收编号必须恰好出现一次。只允许 `not_started`、`partial`、`passed`、`not_applicable`；`passed` 必须填写可访问证据和版本。不要用示例结果冒充验收。

矩阵只汇总结果。业务验收标准写在需求文件，具体场景的前置条件、触发、预期结果和验证方式写在所属功能文件；实际命令、环境、输出和版本写入 `验收证据/`。
