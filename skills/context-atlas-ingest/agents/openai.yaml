interface:
  display_name: "context-atlas-ingest"
  short_description: "只读摄取单个或有限批次来源并生成维护路由"
  default_prompt: "使用 $context-atlas-ingest 只读分析一个或一批可定位来源并输出候选映射。"
policy:
  allow_implicit_invocation: false
