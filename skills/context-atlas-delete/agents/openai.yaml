interface:
  display_name: "context-atlas-delete"
  short_description: "确认后永久删除无审计价值的知识及其引用"
  default_prompt: "使用 $context-atlas-delete 永久删除已确认无审计价值的正式知识，并同步清理引用。"
policy:
  allow_implicit_invocation: false
